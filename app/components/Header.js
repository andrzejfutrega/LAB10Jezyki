export default function Header({ title }) {
    return (
      <header style={{ textAlign: 'center', margin: '1rem 0', fontSize: '2rem', color: '#333' }}>
        {title}
      </header>
    );
  }
  